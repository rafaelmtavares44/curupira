"""Testes do Curupira.

Este pacote existe para que `tests.fabricas` seja importável por um nome único —
sem ele, o mypy encontra o mesmo arquivo como `fabricas` e como `tests.fabricas`
e recusa a análise.
"""
