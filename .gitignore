# Segredos — primeira secao de proposito. Ver SECURITY.md.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.python-version

# Ferramentas
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Artefatos de rodada (resultados brutos ficam fora do git; ver README)
runs/
cache/
*.parquet
!tasks/**/*.parquet

# OneDrive: esta pasta vive dentro do OneDrive.
# O venv nunca deve sincronizar (ja ignorado acima, na secao Python).

# Lixo que o pytest deixa quando o rename do cache falha (pasta sincronizada).
pytest-cache-files-*

# Pasta que o app do Claude cria dentro da pasta conectada quando um arquivo e
# entregue por download. Sao COPIAS de arquivos que ja estao no lugar certo da
# arvore. Commita-las duplicaria codigo no repositorio, e a duplicata nao seria
# vista por portao nenhum: o pytest nao a coleta, o ruff nao a checa, o mypy nao
# a le. Duas versoes do mesmo arquivo, e uma delas sem verificacao.
Claude outputs/

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Held-out: nunca, em hipotese alguma, dentro deste repositorio.
heldout/
tasks/**/held_out/
